/** Today tab accents (screen background uses theme.color.background). */
export const todayTheme = {
  cardBackground: '#FFFFFF',
  accentMuted: 'rgba(0, 0, 0, 0.05)',
  labelCaps: '#912338',
} as const;
